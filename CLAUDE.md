# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

静态个人主页 —— 单文件 HTML 网站，用于展示个人信息、技能和联系方式。面向运营/内容/产品类实习求职。

## Development

没有构建工具或依赖。直接编辑 `index.html`，然后在浏览器中打开即可预览：

```
start index.html
```

所有 CSS 均为内联样式，位于 `<style>` 标签中。修改时直接在文件内编辑即可。
